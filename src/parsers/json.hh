#pragma once


/*! \namespace Json
 * \brief JSON Parser and generator interface.
 *
 * This interface parses JSON strings and creates a dynamic tree
 * structure (using a class hierarchy as opposed to template trees).
 */

#include "json/parse_string.hh"
#include "json/printer.hh"

